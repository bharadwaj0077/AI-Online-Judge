$kind: http-example
name: Login - Happy Path
request:
  url: "{{baseUrl}}/api/v1/auth/login"
  method: POST
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "username": "{{testUsername}}",
        "password": "{{testPassword}}"
      }
response:
  statusCode: 200
  statusText: OK
  body:
    type: json
    content: ""
order: 2010185814102584
